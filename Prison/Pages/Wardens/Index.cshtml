@page
@model Prison.Pages.Wardens.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Warden[0].EmploymentStartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Warden[0].Salary)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Warden[0].FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Warden[0].LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Warden[0].Age)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Warden) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EmploymentStartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Salary)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Age)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
